{
  "name": "natural-language-todo-card",
  "private": true,
  "type": "module",
  "description": "Development tooling only. The card itself has no dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
